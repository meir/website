meta {
  str url = "/"
}

func layout = use("components/layout")

return layout(url, "Home", : main {
  h1 > "Hello world!"
  section {
    div#scroller;
    p > "Welcome to the home page of our application."
  }
})
