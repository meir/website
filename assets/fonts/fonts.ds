
func use_font = (str name) {
  use(std.format("assets/fonts/{}", name))
}

use_font("DinaRemasterII.ttf") // code blocks
use_font("DelaGothicOne-Regular.ttf") // urls 
use_font("IndieFlower-Regular.ttf") // quotes
use_font("Jacquard12-Regular.ttf") // headings
use_font("LibertinusMono-Regular.ttf") // content
use_font("ClimateCrisis-Regular.ttf") // navigation
