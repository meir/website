
str style = use("assets/main.scss")
func navigation = use("components/navigation")

return (str current_url, str title, arr body) element {
  return : html {
    head {
      title > @{title}
      link rel="stylesheet" href=@{style};
    }
    body {
      @{navigation(current_url)}
      main > article > @{body}
    }
  }
}
